[project]
name = "corsair-cloud"
version = "0.1.0"
description = "Thin HTTP client for hosted Corsair Cloud projects"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["corsair_cloud"]
