FROM postgres:16-alpine

# Defaults for local dev (override via docker compose env if desired)
ENV POSTGRES_DB=corsair_demo \
    POSTGRES_USER=postgres \
    POSTGRES_PASSWORD=postgres

EXPOSE 5432


