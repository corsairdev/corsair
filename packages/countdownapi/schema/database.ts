/**
 * No persisted entities in this plugin: every operation is a live search or
 * lookup against eBay's current listings, not a record with a durable
 * identity a caller would look up repeatedly by id.
 */
export {};
