import { historical } from './historical';
import { latest } from './latest';
import { list } from './symbols';

export const Rates = {
	latest,
	historical,
};

export const Symbols = {
	list,
};

export * from './types';
