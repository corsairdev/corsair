import {
	applicationCommand,
	messageComponent,
	modalSubmit,
	ping,
} from './interactions';

export const InteractionWebhooks = {
	ping,
	applicationCommand,
	messageComponent,
	modalSubmit,
};

export * from './types';
