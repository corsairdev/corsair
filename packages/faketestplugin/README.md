# @corsair-dev/faketestplugin

Planted test plugin for review-loop dogfood. Do not merge.

## Auth setup

Provide an API key via plugin options (`key`).

## Endpoints

- `listItems` — fetches items from the FakeTest API.
