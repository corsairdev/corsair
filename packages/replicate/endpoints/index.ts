import {
	createPrediction,
	getPrediction,
	listPredictions,
	cancelPrediction,
} from './example';

export const CreatePrediction = createPrediction;
export const GetPrediction = getPrediction;
export const ListPredictions = listPredictions;
export const CancelPrediction = cancelPrediction;

export * from './types';