import { list as pagesList } from './pages';
import { list as spacesList } from './spaces';

export const Pages = {
	list: pagesList,
};

export const Spaces = {
	list: spacesList,
};

export * from './types';
