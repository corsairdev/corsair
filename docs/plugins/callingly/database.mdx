---
title: Database
description: "Callingly local sync: searchable entities, `.search()` filters, and operators."
---

The Callingly plugin syncs data locally. Use `corsair.callingly.db.<entity>.search({ data, limit?, offset? })` with the filters listed per entity.

<Info>
**New to Corsair?** See [database operations](/concepts/database), [data synchronization](/concepts/integrations), and [multi-tenancy](/concepts/multi-tenancy).
</Info>

## Calls

Path: `callingly.db.calls.search`

```ts
const rows = await corsair.callingly.db.calls.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `started_at` | `string` | equals, contains, startsWith, endsWith, in |
| `direction` | `string` | equals, contains, startsWith, endsWith, in |
| `status` | `string` | equals, contains, startsWith, endsWith, in |
| `status_formatted` | `string` | equals, contains, startsWith, endsWith, in |
| `lead_status` | `string` | equals, contains, startsWith, endsWith, in |
| `lead_status_formatted` | `string` | equals, contains, startsWith, endsWith, in |
| `ring_status` | `string` | equals, contains, startsWith, endsWith, in |
| `seconds` | `number` | equals, gt, gte, lt, lte, in |
| `retry` | `number` | equals, gt, gte, lt, lte, in |
| `lead_retry` | `number` | equals, gt, gte, lt, lte, in |
| `time_formatted` | `string` | equals, contains, startsWith, endsWith, in |
| `from_formatted` | `string` | equals, contains, startsWith, endsWith, in |
| `source` | `string` | equals, contains, startsWith, endsWith, in |
| `recording_url` | `string` | equals, contains, startsWith, endsWith, in |
| `waveform_url` | `string` | equals, contains, startsWith, endsWith, in |
| `phone_number_formatted` | `string` | equals, contains, startsWith, endsWith, in |
| `human_result` | `string` | equals, contains, startsWith, endsWith, in |
| `transcript` | `string` | equals, contains, startsWith, endsWith, in |
| `is_voicemail` | `number` | equals, gt, gte, lt, lte, in |
| `is_queue` | `number` | equals, gt, gte, lt, lte, in |
| `is_team_offline` | `number` | equals, gt, gte, lt, lte, in |
| `is_error` | `number` | equals, gt, gte, lt, lte, in |
| `error_code` | `string` | equals, contains, startsWith, endsWith, in |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

## Clients

Path: `callingly.db.clients.search`

```ts
const rows = await corsair.callingly.db.clients.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `name` | `string` | equals, contains, startsWith, endsWith, in |
| `email` | `string` | equals, contains, startsWith, endsWith, in |
| `billed_users` | `number` | equals, gt, gte, lt, lte, in |
| `billed_numbers` | `number` | equals, gt, gte, lt, lte, in |
| `fname` | `string` | equals, contains, startsWith, endsWith, in |
| `lname` | `string` | equals, contains, startsWith, endsWith, in |
| `company` | `string` | equals, contains, startsWith, endsWith, in |
| `phone_number` | `string` | equals, contains, startsWith, endsWith, in |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

## Leads

Path: `callingly.db.leads.search`

```ts
const rows = await corsair.callingly.db.leads.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `name` | `string` | equals, contains, startsWith, endsWith, in |
| `label` | `string` | equals, contains, startsWith, endsWith, in |
| `fname` | `string` | equals, contains, startsWith, endsWith, in |
| `lname` | `string` | equals, contains, startsWith, endsWith, in |
| `email` | `string` | equals, contains, startsWith, endsWith, in |
| `phone_number` | `string` | equals, contains, startsWith, endsWith, in |
| `phone_number_formatted` | `string` | equals, contains, startsWith, endsWith, in |
| `source` | `string` | equals, contains, startsWith, endsWith, in |
| `crm` | `string` | equals, contains, startsWith, endsWith, in |
| `company` | `string` | equals, contains, startsWith, endsWith, in |
| `category` | `string` | equals, contains, startsWith, endsWith, in |
| `status` | `string` | equals, contains, startsWith, endsWith, in |
| `result` | `string` | equals, contains, startsWith, endsWith, in |
| `scheduled_call_at` | `string` | equals, contains, startsWith, endsWith, in |
| `is_stopped` | `number` | equals, gt, gte, lt, lte, in |
| `is_blocked` | `number` | equals, gt, gte, lt, lte, in |
| `created_at` | `string` | equals, contains, startsWith, endsWith, in |
| `deleted_at` | `string` | equals, contains, startsWith, endsWith, in |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

## Schedules

Path: `callingly.db.schedules.search`

```ts
const rows = await corsair.callingly.db.schedules.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

## Teams

Path: `callingly.db.teams.search`

```ts
const rows = await corsair.callingly.db.teams.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `name` | `string` | equals, contains, startsWith, endsWith, in |
| `is_record` | `number` | equals, gt, gte, lt, lte, in |
| `call_mode` | `string` | equals, contains, startsWith, endsWith, in |
| `whispertext` | `string` | equals, contains, startsWith, endsWith, in |
| `post_whispertext` | `string` | equals, contains, startsWith, endsWith, in |
| `language` | `string` | equals, contains, startsWith, endsWith, in |
| `delay` | `number` | equals, gt, gte, lt, lte, in |
| `is_retry` | `number` | equals, gt, gte, lt, lte, in |
| `retries` | `number` | equals, gt, gte, lt, lte, in |
| `is_reschedule` | `number` | equals, gt, gte, lt, lte, in |
| `is_retry_lead` | `number` | equals, gt, gte, lt, lte, in |
| `lead_retries` | `number` | equals, gt, gte, lt, lte, in |
| `is_sms` | `number` | equals, gt, gte, lt, lte, in |
| `sms_body` | `string` | equals, contains, startsWith, endsWith, in |
| `whispertext_voice` | `string` | equals, contains, startsWith, endsWith, in |
| `is_users_available_for_call` | `boolean` | equals |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

## Team Users

Path: `callingly.db.teamUsers.search`

```ts
const rows = await corsair.callingly.db.teamUsers.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `name` | `string` | equals, contains, startsWith, endsWith, in |
| `priority` | `number` | equals, gt, gte, lt, lte, in |
| `cap` | `number` | equals, gt, gte, lt, lte, in |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

## Users

Path: `callingly.db.users.search`

```ts
const rows = await corsair.callingly.db.users.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `fname` | `string` | equals, contains, startsWith, endsWith, in |
| `lname` | `string` | equals, contains, startsWith, endsWith, in |
| `name` | `string` | equals, contains, startsWith, endsWith, in |
| `email` | `string` | equals, contains, startsWith, endsWith, in |
| `phone_number` | `string` | equals, contains, startsWith, endsWith, in |
| `ext` | `string` | equals, contains, startsWith, endsWith, in |
| `donotdisturb` | `number` | equals, gt, gte, lt, lte, in |
| `priority` | `number` | equals, gt, gte, lt, lte, in |
| `timezone` | `string` | equals, contains, startsWith, endsWith, in |
| `is_available` | `boolean` | equals |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

## Webhooks

Path: `callingly.db.webhooks.search`

```ts
const rows = await corsair.callingly.db.webhooks.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `name` | `string` | equals, contains, startsWith, endsWith, in |
| `event` | `string` | equals, contains, startsWith, endsWith, in |
| `target_url` | `string` | equals, contains, startsWith, endsWith, in |
| `call_status` | `string` | equals, contains, startsWith, endsWith, in |
| `call_lead_status` | `string` | equals, contains, startsWith, endsWith, in |
| `field` | `string` | equals, contains, startsWith, endsWith, in |
| `filter` | `string` | equals, contains, startsWith, endsWith, in |
| `call_direction` | `string` | equals, contains, startsWith, endsWith, in |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

