---
title: Overview
description: "Unione plugin for Corsair"
---

Use **UniOne** through Corsair: one client, typed API calls, optional local DB sync, and incoming webhooks documented below.

**What you get:**

- 30 typed API operations
- 7 database entities synced for fast `.search()` / `.list()` queries
- 2 incoming webhook event types

## Setup

<Steps>

<Step title="Install">
```bash
pnpm install @corsair-dev/unione
```

</Step>

<Step title="Add the plugin">
<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { unione } from '@corsair-dev/unione';

export const corsair = createCorsair({
	// ... other config options,
	multiTenancy: false,
    plugins: [unione()],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { unione } from '@corsair-dev/unione';

export const corsair = createCorsair({
	// ... other config options,
    multiTenancy: true,
    plugins: [unione()],
});
```

See [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Tab>
</Tabs>

</Step>

<Step title="Get credentials">
Follow [Get Credentials](/plugins/unione/get-credentials) if you need help getting keys.

</Step>

<Step title="Store credentials">
<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --plugin=unione
```

Use the key names documented in [Get Credentials](/plugins/unione/get-credentials) (for example `api_key=`, `bot_token=`, or OAuth client fields).
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --plugin=unione --tenant=<tenantId>
```

Store per-tenant secrets after you create the tenant record. See [Multi-tenancy](/concepts/multi-tenancy).
</Tab>
</Tabs>

</Step>

</Steps>

## Authentication

Each tab shows how to register the plugin for that authentication method. The default `authType` from the plugin does not need to appear in the factory call.

<Tabs>
<Tab title="API Key (Default)">

```ts corsair.ts
unione()
```

Store credentials with `pnpm corsair setup --plugin=unione` (see [Get Credentials](/plugins/unione/get-credentials) for field names). For OAuth, you typically store integration keys at the provider level and tokens per account or tenant.

More: [API Key](/concepts/api-key)

</Tab>
</Tabs>

## Webhooks

This plugin registers **2** webhook handler(s). Configure your provider to POST events to your Corsair HTTP endpoint, then use `webhookHooks` in the plugin factory for custom logic.

See [Webhooks](/plugins/unione/webhooks) for every event path and payload shape, and [Webhooks concept](/concepts/webhooks) for how to set up routing.


## Query synced data

Synced entities support `corsair.unione.db.<entity>.search()` and `.list()`. See [Database](/plugins/unione/database) for filters and operators.


## Example API calls

**Read-style (read):** `emailValidation.batch`

```ts
await corsair.unione.api.emailValidation.batch({});
```


**Write-style (destructive):** `domain.delete`

```ts
await corsair.unione.api.domain.delete({});
```


See the full list on the [API](/plugins/unione/api) page. Use `pnpm corsair list --plugin=unione` and `pnpm corsair schema <path>` locally to inspect schemas.

---

## Hooks

Use `hooks` on API calls and `webhookHooks` on incoming events to add logging, approvals, or side effects. See [Hooks](/concepts/hooks) and the [Webhooks](/plugins/unione/webhooks) page for payload types.

---

## Reference

| Topic | Link |
|-------|------|
| API | [API](/plugins/unione/api) |
| Database | [Database](/plugins/unione/database) |
| Webhooks | [Webhooks](/plugins/unione/webhooks) |
| Credentials | [Get credentials](/plugins/unione/get-credentials) |

