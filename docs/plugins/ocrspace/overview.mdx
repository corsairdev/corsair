---
title: Overview
description: "OCR.space plugin for Corsair"
---

Use **OCR.space** through Corsair: one client, typed API calls, optional local DB sync.

**What you get:**

- 3 typed API operations
- 2 database entities synced for fast `.search()` / `.list()` queries

## Setup

<Steps>

<Step title="Install">
```bash
pnpm install @corsair-dev/ocrspace
```

</Step>

<Step title="Add the plugin">
<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { ocrspace } from '@corsair-dev/ocrspace';

export const corsair = createCorsair({
	// ... other config options,
	multiTenancy: false,
    plugins: [ocrspace()],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { ocrspace } from '@corsair-dev/ocrspace';

export const corsair = createCorsair({
	// ... other config options,
    multiTenancy: true,
    plugins: [ocrspace()],
});
```

See [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Tab>
</Tabs>

</Step>

<Step title="Get credentials">
Follow [Get Credentials](/plugins/ocrspace/get-credentials) if you need help getting keys.

</Step>

<Step title="Store credentials">
<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --plugin=ocrspace
```

Use the key names documented in [Get Credentials](/plugins/ocrspace/get-credentials) (for example `api_key=`, `bot_token=`, or OAuth client fields).
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --plugin=ocrspace --tenant=<tenantId>
```

Store per-tenant secrets after you create the tenant record. See [Multi-tenancy](/concepts/multi-tenancy).
</Tab>
</Tabs>

</Step>

</Steps>

## Authentication

Each tab shows how to register the plugin for that authentication method. The default `authType` from the plugin does not need to appear in the factory call.

<Tabs>
<Tab title="API Key (Default)">

```ts corsair.ts
ocrspace()
```

Store credentials with `pnpm corsair setup --plugin=ocrspace` (see [Get Credentials](/plugins/ocrspace/get-credentials) for field names). For OAuth, you typically store integration keys at the provider level and tokens per account or tenant.

More: [API Key](/concepts/api-key)

</Tab>
</Tabs>


## Query synced data

Synced entities support `corsair.ocrspace.db.<entity>.search()` and `.list()`. See [Database](/plugins/ocrspace/database) for filters and operators.


## Example API calls

**Read-style (read):** `account.conversions`

```ts
await corsair.ocrspace.api.account.conversions({});
```


**Write-style (write):** `—`

_No write-style endpoint inferred; pick any operation from the reference below._


See the full list on the [API](/plugins/ocrspace/api) page. Use `pnpm corsair list --plugin=ocrspace` and `pnpm corsair schema <path>` locally to inspect schemas.

---

## Hooks

Use `hooks` on API calls and `webhookHooks` on incoming events to add logging, approvals, or side effects. See [Hooks](/concepts/hooks) and [Webhooks](/concepts/webhooks) for routing and payload patterns.

---

## Reference

| Topic | Link |
|-------|------|
| API | [API](/plugins/ocrspace/api) |
| Database | [Database](/plugins/ocrspace/database) |
| Credentials | [Get credentials](/plugins/ocrspace/get-credentials) |

