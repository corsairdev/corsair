---
title: Overview
description: "ImgBB plugin for Corsair"
---

Use **ImgBB** through Corsair: one client, typed API calls, optional local DB sync.

**What you get:**

- 2 typed API operations
- 1 database entity synced for fast `.search()` / `.list()` queries

## Setup

<Steps>

<Step title="Install">
```bash
pnpm install @corsair-dev/imgbb
```

</Step>

<Step title="Add the plugin">
<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { imgbb } from '@corsair-dev/imgbb';

export const corsair = createCorsair({
	// ... other config options,
	multiTenancy: false,
    plugins: [imgbb()],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { imgbb } from '@corsair-dev/imgbb';

export const corsair = createCorsair({
	// ... other config options,
    multiTenancy: true,
    plugins: [imgbb()],
});
```

See [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Tab>
</Tabs>

</Step>

<Step title="Get credentials">
Follow [Get Credentials](/plugins/imgbb/get-credentials) if you need help getting keys.

</Step>

<Step title="Store credentials">
<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --plugin=imgbb
```

Use the key names documented in [Get Credentials](/plugins/imgbb/get-credentials) (for example `api_key=`, `bot_token=`, or OAuth client fields).
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --plugin=imgbb --tenant=<tenantId>
```

Store per-tenant secrets after you create the tenant record. See [Multi-tenancy](/concepts/multi-tenancy).
</Tab>
</Tabs>

</Step>

</Steps>

## Authentication

Each tab shows how to register the plugin for that authentication method. The default `authType` from the plugin does not need to appear in the factory call.

<Tabs>
<Tab title="API Key (Default)">

```ts corsair.ts
imgbb()
```

Store credentials with `pnpm corsair setup --plugin=imgbb` (see [Get Credentials](/plugins/imgbb/get-credentials) for field names). For OAuth, you typically store integration keys at the provider level and tokens per account or tenant.

More: [API Key](/concepts/api-key)

</Tab>
</Tabs>


## Query synced data

Synced entities support `corsair.imgbb.db.<entity>.search()` and `.list()`. See [Database](/plugins/imgbb/database) for filters and operators.


## Example API calls

**Read-style (read):** `auth.getApiKey`

```ts
await corsair.imgbb.api.auth.getApiKey({});
```


**Write-style (write):** `images.upload`

```ts
await corsair.imgbb.api.images.upload({});
```


See the full list on the [API](/plugins/imgbb/api) page. Use `pnpm corsair list --plugin=imgbb` and `pnpm corsair schema <path>` locally to inspect schemas.

---

## Hooks

Use `hooks` on API calls and `webhookHooks` on incoming events to add logging, approvals, or side effects. See [Hooks](/concepts/hooks) and [Webhooks](/concepts/webhooks) for routing and payload patterns.

---

## Reference

| Topic | Link |
|-------|------|
| API | [API](/plugins/imgbb/api) |
| Database | [Database](/plugins/imgbb/database) |
| Credentials | [Get credentials](/plugins/imgbb/get-credentials) |

