---
title: Overview
description: "Bookingmood plugin for Corsair"
---

Use **Bookingmood** through Corsair: one client, typed API calls, optional local DB sync, and incoming webhooks documented below.

**What you get:**

- 19 typed API operations
- 5 database entities synced for fast `.search()` / `.list()` queries
- 5 incoming webhook event types

## Setup

<Steps>

<Step title="Install">
```bash
pnpm install @corsair-dev/bookingmood
```

</Step>

<Step title="Add the plugin">
<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { bookingmood } from '@corsair-dev/bookingmood';

export const corsair = createCorsair({
	// ... other config options,
	multiTenancy: false,
    plugins: [bookingmood()],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { bookingmood } from '@corsair-dev/bookingmood';

export const corsair = createCorsair({
	// ... other config options,
    multiTenancy: true,
    plugins: [bookingmood()],
});
```

See [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Tab>
</Tabs>

</Step>

<Step title="Get credentials">
Follow [Get Credentials](/plugins/bookingmood/get-credentials) if you need help getting keys.

</Step>

<Step title="Store credentials">
<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --plugin=bookingmood
```

Use the key names documented in [Get Credentials](/plugins/bookingmood/get-credentials) (for example `api_key=`, `bot_token=`, or OAuth client fields).
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --plugin=bookingmood --tenant=<tenantId>
```

Store per-tenant secrets after you create the tenant record. See [Multi-tenancy](/concepts/multi-tenancy).
</Tab>
</Tabs>

</Step>

</Steps>

## Authentication

Each tab shows how to register the plugin for that authentication method. The default `authType` from the plugin does not need to appear in the factory call.

<Tabs>
<Tab title="API Key (Default)">

```ts corsair.ts
bookingmood()
```

Store credentials with `pnpm corsair setup --plugin=bookingmood` (see [Get Credentials](/plugins/bookingmood/get-credentials) for field names). For OAuth, you typically store integration keys at the provider level and tokens per account or tenant.

More: [API Key](/concepts/api-key)

</Tab>
<Tab title="OAuth 2.0">

```ts corsair.ts
bookingmood({
    authType: 'oauth_2',
})
```

Store credentials with `pnpm corsair setup --plugin=bookingmood` (see [Get Credentials](/plugins/bookingmood/get-credentials) for field names). For OAuth, you typically store integration keys at the provider level and tokens per account or tenant.

More: [OAuth 2.0](/concepts/oauth)

</Tab>
</Tabs>

## Webhooks

This plugin registers **5** webhook handler(s). Configure your provider to POST events to your Corsair HTTP endpoint, then use `webhookHooks` in the plugin factory for custom logic.

See [Webhooks](/plugins/bookingmood/webhooks) for every event path and payload shape, and [Webhooks concept](/concepts/webhooks) for how to set up routing.


## Query synced data

Synced entities support `corsair.bookingmood.db.<entity>.search()` and `.list()`. See [Database](/plugins/bookingmood/database) for filters and operators.


## Example API calls

**Read-style (read):** `bookings.get`

```ts
await corsair.bookingmood.api.bookings.get({});
```


**Write-style (write):** `bookings.create`

```ts
await corsair.bookingmood.api.bookings.create({});
```


See the full list on the [API](/plugins/bookingmood/api) page. Use `pnpm corsair list --plugin=bookingmood` and `pnpm corsair schema <path>` locally to inspect schemas.

---

## Hooks

Use `hooks` on API calls and `webhookHooks` on incoming events to add logging, approvals, or side effects. See [Hooks](/concepts/hooks) and the [Webhooks](/plugins/bookingmood/webhooks) page for payload types.

---

## Reference

| Topic | Link |
|-------|------|
| API | [API](/plugins/bookingmood/api) |
| Database | [Database](/plugins/bookingmood/database) |
| Webhooks | [Webhooks](/plugins/bookingmood/webhooks) |
| Credentials | [Get credentials](/plugins/bookingmood/get-credentials) |

