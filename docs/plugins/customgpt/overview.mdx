---
title: Overview
description: "CustomGPT plugin for Corsair"
---

Use **CustomGPT** through Corsair: one client, typed API calls, optional local DB sync.

**What you get:**

- 40 typed API operations
- 7 database entities synced for fast `.search()` / `.list()` queries

## Setup

<Steps>

<Step title="Install">
```bash
pnpm install @corsair-dev/customgpt
```

</Step>

<Step title="Add the plugin">
<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { customgpt } from '@corsair-dev/customgpt';

export const corsair = createCorsair({
	// ... other config options,
	multiTenancy: false,
    plugins: [customgpt()],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { customgpt } from '@corsair-dev/customgpt';

export const corsair = createCorsair({
	// ... other config options,
    multiTenancy: true,
    plugins: [customgpt()],
});
```

See [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Tab>
</Tabs>

</Step>

<Step title="Get credentials">
Follow [Get Credentials](/plugins/customgpt/get-credentials) if you need help getting keys.

</Step>

<Step title="Store credentials">
<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --plugin=customgpt
```

Use the key names documented in [Get Credentials](/plugins/customgpt/get-credentials) (for example `api_key=`, `bot_token=`, or OAuth client fields).
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --plugin=customgpt --tenant=<tenantId>
```

Store per-tenant secrets after you create the tenant record. See [Multi-tenancy](/concepts/multi-tenancy).
</Tab>
</Tabs>

</Step>

</Steps>

## Authentication

Each tab shows how to register the plugin for that authentication method. The default `authType` from the plugin does not need to appear in the factory call.

<Tabs>
<Tab title="API Key (Default)">

```ts corsair.ts
customgpt()
```

Store credentials with `pnpm corsair setup --plugin=customgpt` (see [Get Credentials](/plugins/customgpt/get-credentials) for field names). For OAuth, you typically store integration keys at the provider level and tokens per account or tenant.

More: [API Key](/concepts/api-key)

</Tab>
</Tabs>


## Query synced data

Synced entities support `corsair.customgpt.db.<entity>.search()` and `.list()`. See [Database](/plugins/customgpt/database) for filters and operators.


## Example API calls

**Read-style (read):** `licenses.get`

```ts
await corsair.customgpt.api.licenses.get({});
```


**Write-style (write):** `conversations.create`

```ts
await corsair.customgpt.api.conversations.create({});
```


See the full list on the [API](/plugins/customgpt/api) page. Use `pnpm corsair list --plugin=customgpt` and `pnpm corsair schema <path>` locally to inspect schemas.

---

## Hooks

Use `hooks` on API calls and `webhookHooks` on incoming events to add logging, approvals, or side effects. See [Hooks](/concepts/hooks) and [Webhooks](/concepts/webhooks) for routing and payload patterns.

---

## Reference

| Topic | Link |
|-------|------|
| API | [API](/plugins/customgpt/api) |
| Database | [Database](/plugins/customgpt/database) |
| Credentials | [Get credentials](/plugins/customgpt/get-credentials) |

