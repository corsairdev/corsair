---
title: Get Credentials
description: Step-by-step instructions for obtaining a Merriam-Webster Collegiate Dictionary API key.
---

This guide walks you through obtaining the credential required for the Dictionary plugin.

## Authentication Method

The Dictionary plugin uses API key authentication.

- **[`api_key`](/concepts/api-key)** (default) - API key authentication

## API Key Setup

### Step 1: Get API Key

1. Create a free account at the [Merriam-Webster Developer Center](https://dictionaryapi.com/register/index)
2. Under **Request a Key**, select the **Collegiate Dictionary** API (not Thesaurus, Medical, or Spanish)
3. Submit the registration form
4. Merriam-Webster emails you the API key
5. **Important**: Store the key securely — the free tier is rate-limited per key

**Storing Credentials:**

The preferred method is to store the API key in the database using the keys API:

```ts
await corsair
    .withTenant('default')
    .dictionary.keys.setApiKey('your-api-key');
```

Alternatively, you can provide the key directly in the plugin configuration:

```ts corsair.ts
dictionary({
    authType: "api_key",
    key: process.env.DICTIONARY_API_KEY,
})
```

## Required Credentials Summary

| Credential | Required For | Where to Find |
|-----------|--------------|---------------|
| API Key | API Key auth | [dictionaryapi.com](https://dictionaryapi.com/register/index) → Collegiate Dictionary |

For general information about how Corsair handles authentication, see [Authentication](/concepts/auth).
