---
title: Overview
description: "Kaggle plugin for Corsair"
---

Use **Kaggle** through Corsair: one client, typed API calls, optional local DB sync.

**What you get:**

- 25 typed API operations
- 4 database entities synced for fast `.search()` / `.list()` queries

## Setup

<Steps>

<Step title="Install">
```bash
pnpm install @corsair-dev/kaggle
```

</Step>

<Step title="Add the plugin">
<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { kaggle } from '@corsair-dev/kaggle';

export const corsair = createCorsair({
	// ... other config options,
	multiTenancy: false,
    plugins: [kaggle()],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { kaggle } from '@corsair-dev/kaggle';

export const corsair = createCorsair({
	// ... other config options,
    multiTenancy: true,
    plugins: [kaggle()],
});
```

See [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Tab>
</Tabs>

</Step>

<Step title="Get credentials">
Follow [Get Credentials](/plugins/kaggle/get-credentials) if you need help getting keys.

</Step>

<Step title="Store credentials">
<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --plugin=kaggle
```

Use the key names documented in [Get Credentials](/plugins/kaggle/get-credentials) (for example `api_key=`, `bot_token=`, or OAuth client fields).
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --plugin=kaggle --tenant=<tenantId>
```

Store per-tenant secrets after you create the tenant record. See [Multi-tenancy](/concepts/multi-tenancy).
</Tab>
</Tabs>

</Step>

</Steps>

## Authentication

Each tab shows how to register the plugin for that authentication method. The default `authType` from the plugin does not need to appear in the factory call.

<Tabs>
<Tab title="API Key (Default)">

```ts corsair.ts
kaggle()
```

Store credentials with `pnpm corsair setup --plugin=kaggle` (see [Get Credentials](/plugins/kaggle/get-credentials) for field names). For OAuth, you typically store integration keys at the provider level and tokens per account or tenant.

More: [API Key](/concepts/api-key)

</Tab>
</Tabs>


## Query synced data

Synced entities support `corsair.kaggle.db.<entity>.search()` and `.list()`. See [Database](/plugins/kaggle/database) for filters and operators.


## Example API calls

**Read-style (read):** `competitions.downloadFile`

```ts
await corsair.kaggle.api.competitions.downloadFile({});
```


**Write-style (write):** `competitions.generateSubmissionUrl`

```ts
await corsair.kaggle.api.competitions.generateSubmissionUrl({});
```


See the full list on the [API](/plugins/kaggle/api) page. Use `pnpm corsair list --plugin=kaggle` and `pnpm corsair schema <path>` locally to inspect schemas.

---

## Hooks

Use `hooks` on API calls and `webhookHooks` on incoming events to add logging, approvals, or side effects. See [Hooks](/concepts/hooks) and [Webhooks](/concepts/webhooks) for routing and payload patterns.

---

## Reference

| Topic | Link |
|-------|------|
| API | [API](/plugins/kaggle/api) |
| Database | [Database](/plugins/kaggle/database) |
| Credentials | [Get credentials](/plugins/kaggle/get-credentials) |

