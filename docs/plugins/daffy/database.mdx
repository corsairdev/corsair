---
title: Database
description: "Daffy reads data directly from its public API."
---

The Daffy plugin does not persist API data locally and exposes no database entities. Use its typed API operations to retrieve current Daffy data.
