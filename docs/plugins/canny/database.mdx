---
title: Database
description: "Canny local sync: searchable entities, `.search()` filters, and operators."
---

The Canny plugin syncs data locally. Use `corsair.canny.db.<entity>.search({ data, limit?, offset? })` with the filters listed per entity.

<Info>
**New to Corsair?** See [database operations](/concepts/database), [data synchronization](/concepts/integrations), and [multi-tenancy](/concepts/multi-tenancy).
</Info>

## Boards

Path: `canny.db.boards.search`

```ts
const rows = await corsair.canny.db.boards.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `id` | `string` | equals, contains, startsWith, endsWith, in |
| `name` | `string` | equals, contains, startsWith, endsWith, in |
| `created` | `date` | equals, before, after, between |
| `isPrivate` | `boolean` | equals |
| `postCount` | `number` | equals, gt, gte, lt, lte, in |
| `privateComments` | `boolean` | equals |
| `url` | `string` | equals, contains, startsWith, endsWith, in |
| `token` | `string` | equals, contains, startsWith, endsWith, in |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

## Comments

Path: `canny.db.comments.search`

```ts
const rows = await corsair.canny.db.comments.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `id` | `string` | equals, contains, startsWith, endsWith, in |
| `value` | `string` | equals, contains, startsWith, endsWith, in |
| `created` | `date` | equals, before, after, between |
| `postID` | `string` | equals, contains, startsWith, endsWith, in |
| `authorID` | `string` | equals, contains, startsWith, endsWith, in |
| `parentID` | `string` | equals, contains, startsWith, endsWith, in |
| `internal` | `boolean` | equals |
| `likeCount` | `number` | equals, gt, gte, lt, lte, in |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

## Posts

Path: `canny.db.posts.search`

```ts
const rows = await corsair.canny.db.posts.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `id` | `string` | equals, contains, startsWith, endsWith, in |
| `title` | `string` | equals, contains, startsWith, endsWith, in |
| `details` | `string` | equals, contains, startsWith, endsWith, in |
| `score` | `number` | equals, gt, gte, lt, lte, in |
| `status` | `string` | equals, contains, startsWith, endsWith, in |
| `created` | `date` | equals, before, after, between |
| `url` | `string` | equals, contains, startsWith, endsWith, in |
| `commentCount` | `number` | equals, gt, gte, lt, lte, in |
| `eta` | `string` | equals, contains, startsWith, endsWith, in |
| `boardID` | `string` | equals, contains, startsWith, endsWith, in |
| `authorID` | `string` | equals, contains, startsWith, endsWith, in |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

## Votes

Path: `canny.db.votes.search`

```ts
const rows = await corsair.canny.db.votes.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `id` | `string` | equals, contains, startsWith, endsWith, in |
| `created` | `date` | equals, before, after, between |
| `postID` | `string` | equals, contains, startsWith, endsWith, in |
| `voterID` | `string` | equals, contains, startsWith, endsWith, in |
| `boardID` | `string` | equals, contains, startsWith, endsWith, in |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

