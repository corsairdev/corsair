---
title: Overview
description: "Harvest plugin for Corsair"
---

Use **Harvest** through Corsair: one client, typed API calls, optional local DB sync.

**What you get:**

- 57 typed API operations
- 10 database entities synced for fast `.search()` / `.list()` queries

## Setup

<Steps>

<Step title="Install">
```bash
pnpm install @corsair-dev/harvest
```

</Step>

<Step title="Add the plugin">
<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { harvest } from '@corsair-dev/harvest';

export const corsair = createCorsair({
	// ... other config options,
	multiTenancy: false,
    plugins: [harvest()],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { harvest } from '@corsair-dev/harvest';

export const corsair = createCorsair({
	// ... other config options,
    multiTenancy: true,
    plugins: [harvest()],
});
```

See [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Tab>
</Tabs>

</Step>

<Step title="Get credentials">
Follow [Get Credentials](/plugins/harvest/get-credentials) if you need help getting keys.

</Step>

<Step title="Store credentials">
<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --plugin=harvest
```

Use the key names documented in [Get Credentials](/plugins/harvest/get-credentials) (for example `api_key=`, `bot_token=`, or OAuth client fields).
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --plugin=harvest --tenant=<tenantId>
```

Store per-tenant secrets after you create the tenant record. See [Multi-tenancy](/concepts/multi-tenancy).
</Tab>
</Tabs>

</Step>

</Steps>

## Authentication

Each tab shows how to register the plugin for that authentication method. The default `authType` from the plugin does not need to appear in the factory call.

<Tabs>
<Tab title="OAuth 2.0 (Default)">

```ts corsair.ts
harvest()
```

Store credentials with `pnpm corsair setup --plugin=harvest` (see [Get Credentials](/plugins/harvest/get-credentials) for field names). For OAuth, you typically store integration keys at the provider level and tokens per account or tenant.

More: [OAuth 2.0](/concepts/oauth)

</Tab>
</Tabs>


## Query synced data

Synced entities support `corsair.harvest.db.<entity>.search()` and `.list()`. See [Database](/plugins/harvest/database) for filters and operators.


## Example API calls

**Read-style (read):** `clients.get`

```ts
await corsair.harvest.api.clients.get({});
```


**Write-style (write):** `clients.create`

```ts
await corsair.harvest.api.clients.create({});
```


See the full list on the [API](/plugins/harvest/api) page. Use `pnpm corsair list --plugin=harvest` and `pnpm corsair schema <path>` locally to inspect schemas.

---

## Hooks

Use `hooks` on API calls and `webhookHooks` on incoming events to add logging, approvals, or side effects. See [Hooks](/concepts/hooks) and [Webhooks](/concepts/webhooks) for routing and payload patterns.

---

## Reference

| Topic | Link |
|-------|------|
| API | [API](/plugins/harvest/api) |
| Database | [Database](/plugins/harvest/database) |
| Credentials | [Get credentials](/plugins/harvest/get-credentials) |

