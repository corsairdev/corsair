---
title: Overview
description: "Alchemy plugin for Corsair"
---

Use **Alchemy** through Corsair: one client, typed API calls, optional local DB sync.

**What you get:**

- 36 typed API operations
- 5 database entities synced for fast `.search()` / `.list()` queries

## Setup

<Steps>

<Step title="Install">
```bash
pnpm install @corsair-dev/alchemy
```

</Step>

<Step title="Add the plugin">
<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { alchemy } from '@corsair-dev/alchemy';

export const corsair = createCorsair({
	// ... other config options,
	multiTenancy: false,
    plugins: [alchemy()],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { alchemy } from '@corsair-dev/alchemy';

export const corsair = createCorsair({
	// ... other config options,
    multiTenancy: true,
    plugins: [alchemy()],
});
```

See [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Tab>
</Tabs>

</Step>

<Step title="Get credentials">
Follow [Get Credentials](/plugins/alchemy/get-credentials) if you need help getting keys.

</Step>

<Step title="Store credentials">
<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --plugin=alchemy
```

Use the key names documented in [Get Credentials](/plugins/alchemy/get-credentials) (for example `api_key=`, `bot_token=`, or OAuth client fields).
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --plugin=alchemy --tenant=<tenantId>
```

Store per-tenant secrets after you create the tenant record. See [Multi-tenancy](/concepts/multi-tenancy).
</Tab>
</Tabs>

</Step>

</Steps>

## Authentication

Each tab shows how to register the plugin for that authentication method. The default `authType` from the plugin does not need to appear in the factory call.

<Tabs>
<Tab title="API Key (Default)">

```ts corsair.ts
alchemy()
```

Store credentials with `pnpm corsair setup --plugin=alchemy` (see [Get Credentials](/plugins/alchemy/get-credentials) for field names). For OAuth, you typically store integration keys at the provider level and tokens per account or tenant.

More: [API Key](/concepts/api-key)

</Tab>
</Tabs>


## Query synced data

Synced entities support `corsair.alchemy.db.<entity>.search()` and `.list()`. See [Database](/plugins/alchemy/database) for filters and operators.


## Example API calls

**Read-style (read):** `nft.computeRarityV3`

```ts
await corsair.alchemy.api.nft.computeRarityV3({});
```


**Write-style (write):** `nft.invalidateContractV3`

```ts
await corsair.alchemy.api.nft.invalidateContractV3({});
```


See the full list on the [API](/plugins/alchemy/api) page. Use `pnpm corsair list --plugin=alchemy` and `pnpm corsair schema <path>` locally to inspect schemas.

---

## Hooks

Use `hooks` on API calls and `webhookHooks` on incoming events to add logging, approvals, or side effects. See [Hooks](/concepts/hooks) and [Webhooks](/concepts/webhooks) for routing and payload patterns.

---

## Reference

| Topic | Link |
|-------|------|
| API | [API](/plugins/alchemy/api) |
| Database | [Database](/plugins/alchemy/database) |
| Credentials | [Get credentials](/plugins/alchemy/get-credentials) |

