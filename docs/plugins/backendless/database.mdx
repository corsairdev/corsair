---
title: Database
description: "Backendless local sync: searchable entities, `.search()` filters, and operators."
---

The Backendless plugin syncs data locally. Use `corsair.backendless.db.<entity>.search({ data, limit?, offset? })` with the filters listed per entity.

<Info>
**New to Corsair?** See [database operations](/concepts/database), [data synchronization](/concepts/integrations), and [multi-tenancy](/concepts/multi-tenancy).
</Info>

## Data Objects

Path: `backendless.db.dataObjects.search`

```ts
const tenant = corsair.withTenant('acme');
const rows = await tenant.backendless.db.dataObjects.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `objectId` | `string` | equals, contains, startsWith, endsWith, in |
| `___class` | `string` | equals, contains, startsWith, endsWith, in |
| `created` | `number` | equals, gt, gte, lt, lte, in |
| `updated` | `number` | equals, gt, gte, lt, lte, in |
| `ownerId` | `string` | equals, contains, startsWith, endsWith, in |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

## Files

Path: `backendless.db.files.search`

```ts
const tenant = corsair.withTenant('acme');
const rows = await tenant.backendless.db.files.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `name` | `string` | equals, contains, startsWith, endsWith, in |
| `createdOn` | `number` | equals, gt, gte, lt, lte, in |
| `publicUrl` | `string` | equals, contains, startsWith, endsWith, in |
| `size` | `number` | equals, gt, gte, lt, lte, in |
| `url` | `string` | equals, contains, startsWith, endsWith, in |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

## Messages

Path: `backendless.db.messages.search`

```ts
const tenant = corsair.withTenant('acme');
const rows = await tenant.backendless.db.messages.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `errorMessage` | `string` | equals, contains, startsWith, endsWith, in |
| `messageId` | `string` | equals, contains, startsWith, endsWith, in |
| `status` | `string` | equals, contains, startsWith, endsWith, in |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

## Users

Path: `backendless.db.users.search`

```ts
const tenant = corsair.withTenant('acme');
const rows = await tenant.backendless.db.users.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `objectId` | `string` | equals, contains, startsWith, endsWith, in |
| `email` | `string` | equals, contains, startsWith, endsWith, in |
| `___class` | `string` | equals, contains, startsWith, endsWith, in |
| `created` | `number` | equals, gt, gte, lt, lte, in |
| `updated` | `number` | equals, gt, gte, lt, lte, in |
| `ownerId` | `string` | equals, contains, startsWith, endsWith, in |
| `user-token` | `string` | equals, contains, startsWith, endsWith, in |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

