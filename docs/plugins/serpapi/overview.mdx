---
title: Overview
description: "Serpapi plugin for Corsair"
---

Use **Serpapi** through Corsair: one client, typed API calls, optional local DB sync.

**What you get:**

- 48 typed API operations

## Setup

<Steps>

<Step title="Install">
```bash
pnpm install @corsair-dev/serpapi
```

</Step>

<Step title="Add the plugin">
<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { serpapi } from '@corsair-dev/serpapi';

export const corsair = createCorsair({
	// ... other config options,
	multiTenancy: false,
    plugins: [serpapi()],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { serpapi } from '@corsair-dev/serpapi';

export const corsair = createCorsair({
	// ... other config options,
    multiTenancy: true,
    plugins: [serpapi()],
});
```

See [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Tab>
</Tabs>

</Step>

<Step title="Get credentials">
Follow [Get Credentials](/plugins/serpapi/get-credentials) if you need help getting keys.

</Step>

<Step title="Store credentials">
<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --plugin=serpapi
```

Use the key names documented in [Get Credentials](/plugins/serpapi/get-credentials) (for example `api_key=`, `bot_token=`, or OAuth client fields).
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --plugin=serpapi --tenant=<tenantId>
```

Store per-tenant secrets after you create the tenant record. See [Multi-tenancy](/concepts/multi-tenancy).
</Tab>
</Tabs>

</Step>

</Steps>

## Authentication

Each tab shows how to register the plugin for that authentication method. The default `authType` from the plugin does not need to appear in the factory call.

<Tabs>
<Tab title="API Key (Default)">

```ts corsair.ts
serpapi()
```

Store credentials with `pnpm corsair setup --plugin=serpapi` (see [Get Credentials](/plugins/serpapi/get-credentials) for field names). For OAuth, you typically store integration keys at the provider level and tokens per account or tenant.

More: [API Key](/concepts/api-key)

</Tab>
</Tabs>



## Example API calls

**Read-style (read):** `engines.baiduSearch`

```ts
await corsair.serpapi.api.engines.baiduSearch({});
```


**Write-style (write):** `—`

_No write-style endpoint inferred; pick any operation from the reference below._


See the full list on the [API](/plugins/serpapi/api) page. Use `pnpm corsair list --plugin=serpapi` and `pnpm corsair schema <path>` locally to inspect schemas.

---

## Hooks

Use `hooks` on API calls and `webhookHooks` on incoming events to add logging, approvals, or side effects. See [Hooks](/concepts/hooks) and [Webhooks](/concepts/webhooks) for routing and payload patterns.

---

## Reference

| Topic | Link |
|-------|------|
| API | [API](/plugins/serpapi/api) |
| Credentials | [Get credentials](/plugins/serpapi/get-credentials) |

