---
title: Overview
description: "Trello plugin for Corsair"
---

Use **Trello** through Corsair: one client, typed API calls, optional local DB sync, and incoming webhooks documented below.

**What you get:**

- 25 typed API operations
- 5 database entities synced for fast `.search()` / `.list()` queries
- 6 incoming webhook event types

## Setup

<Steps>

<Step title="Install">
```bash
pnpm install @corsair-dev/trello
```

</Step>

<Step title="Add the plugin">
<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { trello } from '@corsair-dev/trello';

export const corsair = createCorsair({
	// ... other config options,
	multiTenancy: false,
    plugins: [trello()],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { trello } from '@corsair-dev/trello';

export const corsair = createCorsair({
	// ... other config options,
    multiTenancy: true,
    plugins: [trello()],
});
```

See [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Tab>
</Tabs>

</Step>

<Step title="Get credentials">
Follow [Get Credentials](/plugins/trello/get-credentials) if you need help getting keys.

</Step>

<Step title="Store credentials">
<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --plugin=trello
```

Use the key names documented in [Get Credentials](/plugins/trello/get-credentials) (for example `api_key=`, `bot_token=`, or OAuth client fields).
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --plugin=trello --tenant=<tenantId>
```

Store per-tenant secrets after you create the tenant record. See [Multi-tenancy](/concepts/multi-tenancy).
</Tab>
</Tabs>

</Step>

</Steps>

## Authentication

Each tab shows how to register the plugin for that authentication method. The default `authType` from the plugin does not need to appear in the factory call.

<Tabs>
<Tab title="API Key (Default)">

```ts corsair.ts
trello()
```

Store credentials with `pnpm corsair setup --plugin=trello` (see [Get Credentials](/plugins/trello/get-credentials) for field names). For OAuth, you typically store integration keys at the provider level and tokens per account or tenant.

More: [API Key](/concepts/api-key)

</Tab>
</Tabs>

## Webhooks

This plugin registers **6** webhook handler(s). Configure your provider to POST events to your Corsair HTTP endpoint, then use `webhookHooks` in the plugin factory for custom logic.

See [Webhooks](/plugins/trello/webhooks) for every event path and payload shape, and [Webhooks concept](/concepts/webhooks) for how to set up routing.


## Query synced data

Synced entities support `corsair.trello.db.<entity>.search()` and `.list()`. See [Database](/plugins/trello/database) for filters and operators.


## Example API calls

**Read-style (read):** `boards.get`

```ts
await corsair.trello.api.boards.get({});
```


**Write-style (write):** `boards.create`

```ts
await corsair.trello.api.boards.create({});
```


See the full list on the [API](/plugins/trello/api) page. Use `pnpm corsair list --plugin=trello` and `pnpm corsair schema <path>` locally to inspect schemas.

---

## Hooks

Use `hooks` on API calls and `webhookHooks` on incoming events to add logging, approvals, or side effects. See [Hooks](/concepts/hooks) and the [Webhooks](/plugins/trello/webhooks) page for payload types.

---

## Reference

| Topic | Link |
|-------|------|
| API | [API](/plugins/trello/api) |
| Database | [Database](/plugins/trello/database) |
| Webhooks | [Webhooks](/plugins/trello/webhooks) |
| Credentials | [Get credentials](/plugins/trello/get-credentials) |

