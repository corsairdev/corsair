---
title: Overview
description: "Twitter plugin for Corsair"
---

Use **Twitter** through Corsair: one client, typed API calls, optional local DB sync.

**What you get:**

- 2 typed API operations
- 1 database entity synced for fast `.search()` / `.list()` queries

## Setup

<Steps>

<Step title="Install">
```bash
pnpm install @corsair-dev/twitter
```

</Step>

<Step title="Add the plugin">
<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { twitter } from '@corsair-dev/twitter';

export const corsair = createCorsair({
	// ... other config options,
	multiTenancy: false,
    plugins: [twitter()],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { twitter } from '@corsair-dev/twitter';

export const corsair = createCorsair({
	// ... other config options,
    multiTenancy: true,
    plugins: [twitter()],
});
```

See [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Tab>
</Tabs>

</Step>

<Step title="Get credentials">
Follow [Get Credentials](/plugins/twitter/get-credentials) if you need help getting keys.

</Step>

<Step title="Store credentials">
<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --plugin=twitter
```

Use the key names documented in [Get Credentials](/plugins/twitter/get-credentials) (for example `api_key=`, `bot_token=`, or OAuth client fields).
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --plugin=twitter --tenant=<tenantId>
```

Store per-tenant secrets after you create the tenant record. See [Multi-tenancy](/concepts/multi-tenancy).
</Tab>
</Tabs>

</Step>

</Steps>

## Authentication

Each tab shows how to register the plugin for that authentication method. The default `authType` from the plugin does not need to appear in the factory call.

<Tabs>
<Tab title="OAuth 2.0 (Default)">

```ts corsair.ts
twitter()
```

Store credentials with `pnpm corsair setup --plugin=twitter` (see [Get Credentials](/plugins/twitter/get-credentials) for field names). For OAuth, you typically store integration keys at the provider level and tokens per account or tenant.

More: [OAuth 2.0](/concepts/oauth)

</Tab>
</Tabs>


## Query synced data

Synced entities support `corsair.twitter.db.<entity>.search()` and `.list()`. See [Database](/plugins/twitter/database) for filters and operators.


## Example API calls

**Read-style (read):** `—`

_No read-style endpoint inferred; pick any operation from the reference below._


**Write-style (write):** `tweets.create`

```ts
await corsair.twitter.api.tweets.create({});
```


See the full list on the [API](/plugins/twitter/api) page. Use `pnpm corsair list --plugin=twitter` and `pnpm corsair schema <path>` locally to inspect schemas.

---

## Hooks

Use `hooks` on API calls and `webhookHooks` on incoming events to add logging, approvals, or side effects. See [Hooks](/concepts/hooks) and [Webhooks](/concepts/webhooks) for routing and payload patterns.

---

## Reference

| Topic | Link |
|-------|------|
| API | [API](/plugins/twitter/api) |
| Database | [Database](/plugins/twitter/database) |
| Credentials | [Get credentials](/plugins/twitter/get-credentials) |

