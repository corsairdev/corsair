---
title: Overview
description: "Bitbucket Cloud OAuth 2.0 plugin for Corsair"
---

Use **Bitbucket Cloud OAuth 2.0** through Corsair: one client, typed API calls, optional local DB sync.

**What you get:**

- 104 typed API operations
- 11 database entities synced for fast `.search()` / `.list()` queries

## Setup

<Steps>

<Step title="Install">
```bash
pnpm install @corsair-dev/bitbucket
```

</Step>

<Step title="Add the plugin">
<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { bitbucket } from '@corsair-dev/bitbucket';

export const corsair = createCorsair({
	// ... other config options,
	multiTenancy: false,
    plugins: [bitbucket()],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { bitbucket } from '@corsair-dev/bitbucket';

export const corsair = createCorsair({
	// ... other config options,
    multiTenancy: true,
    plugins: [bitbucket()],
});
```

See [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Tab>
</Tabs>

</Step>

<Step title="Get credentials">
Follow [Get Credentials](/plugins/bitbucket/get-credentials) if you need help getting keys.

</Step>

<Step title="Store credentials">
<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --plugin=bitbucket
```

Use the key names documented in [Get Credentials](/plugins/bitbucket/get-credentials) (for example `api_key=`, `bot_token=`, or OAuth client fields).
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --plugin=bitbucket --tenant=<tenantId>
```

Store per-tenant secrets after you create the tenant record. See [Multi-tenancy](/concepts/multi-tenancy).
</Tab>
</Tabs>

</Step>

</Steps>

## Authentication

Each tab shows how to register the plugin for that authentication method. The default `authType` from the plugin does not need to appear in the factory call.

<Tabs>
<Tab title="OAuth 2.0 (Default)">

```ts corsair.ts
bitbucket()
```

Store credentials with `pnpm corsair setup --plugin=bitbucket` (see [Get Credentials](/plugins/bitbucket/get-credentials) for field names). For OAuth, you typically store integration keys at the provider level and tokens per account or tenant.

More: [OAuth 2.0](/concepts/oauth)

</Tab>
</Tabs>


## Query synced data

Synced entities support `corsair.bitbucket.db.<entity>.search()` and `.list()`. See [Database](/plugins/bitbucket/database) for filters and operators.


## Example API calls

**Read-style (read):** `commitsAndInsights.getCommitBuildStatus`

```ts
await corsair.bitbucket.api.commitsAndInsights.getCommitBuildStatus({});
```


**Write-style (write):** `commitsAndInsights.createRepositoriesCommitReportsAnnotations`

```ts
await corsair.bitbucket.api.commitsAndInsights.createRepositoriesCommitReportsAnnotations({});
```


See the full list on the [API](/plugins/bitbucket/api) page. Use `pnpm corsair list --plugin=bitbucket` and `pnpm corsair schema <path>` locally to inspect schemas.

---

## Hooks

Use `hooks` on API calls and `webhookHooks` on incoming events to add logging, approvals, or side effects. See [Hooks](/concepts/hooks) and [Webhooks](/concepts/webhooks) for routing and payload patterns.

---

## Reference

| Topic | Link |
|-------|------|
| API | [API](/plugins/bitbucket/api) |
| Database | [Database](/plugins/bitbucket/database) |
| Credentials | [Get credentials](/plugins/bitbucket/get-credentials) |

