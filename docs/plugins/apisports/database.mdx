---
title: Database
description: "API-Sports local sync: searchable entities, `.search()` filters, and operators."
---

The API-Sports plugin syncs data locally. Use `corsair.apisports.db.<entity>.search({ data, limit?, offset? })` with the filters listed per entity.

<Info>
**New to Corsair?** See [database operations](/concepts/database), [data synchronization](/concepts/integrations), and [multi-tenancy](/concepts/multi-tenancy).
</Info>

## Queries

Path: `apisports.db.queries.search`

```ts
const rows = await corsair.apisports.db.queries.search({
    data: { /* filters below */ },
    limit: 100,
    offset: 0,
});
```

### Searchable filters

| Field | Type | Operators |
|-------|------|-----------|
| `entity_id` | `string` | equals, contains, startsWith, endsWith, in |
| `sport` | `string` | equals, contains, startsWith, endsWith, in |
| `path` | `string` | equals, contains, startsWith, endsWith, in |
| `queriedAt` | `date` | equals, before, after, between |

_Every `.search()` also accepts `limit` and `offset` for pagination. `.list()` is available on the same path without the `.search` suffix in code — see [database operations](/concepts/database)._

---

