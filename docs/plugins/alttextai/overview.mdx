---
title: Overview
description: "AltText.ai plugin for Corsair"
---

Use **AltText.ai** through Corsair: one client, typed API calls, optional local DB sync.

**What you get:**

- 10 typed API operations
- 1 database entity synced for fast `.search()` / `.list()` queries

## Setup

<Steps>

<Step title="Install">
```bash
pnpm install @corsair-dev/alttextai
```

</Step>

<Step title="Add the plugin">
<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { alttextai } from '@corsair-dev/alttextai';

export const corsair = createCorsair({
	// ... other config options,
	multiTenancy: false,
    plugins: [alttextai()],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { alttextai } from '@corsair-dev/alttextai';

export const corsair = createCorsair({
	// ... other config options,
    multiTenancy: true,
    plugins: [alttextai()],
});
```

See [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Tab>
</Tabs>

</Step>

<Step title="Get credentials">
Follow [Get Credentials](/plugins/alttextai/get-credentials) if you need help getting keys.

</Step>

<Step title="Store credentials">
<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --plugin=alttextai
```

Use the key names documented in [Get Credentials](/plugins/alttextai/get-credentials) (for example `api_key=`, `bot_token=`, or OAuth client fields).
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --plugin=alttextai --tenant=<tenantId>
```

Store per-tenant secrets after you create the tenant record. See [Multi-tenancy](/concepts/multi-tenancy).
</Tab>
</Tabs>

</Step>

</Steps>

## Authentication

Each tab shows how to register the plugin for that authentication method. The default `authType` from the plugin does not need to appear in the factory call.

<Tabs>
<Tab title="API Key (Default)">

```ts corsair.ts
alttextai()
```

Store credentials with `pnpm corsair setup --plugin=alttextai` (see [Get Credentials](/plugins/alttextai/get-credentials) for field names). For OAuth, you typically store integration keys at the provider level and tokens per account or tenant.

More: [API Key](/concepts/api-key)

</Tab>
</Tabs>


## Query synced data

Synced entities support `corsair.alttextai.db.<entity>.search()` and `.list()`. See [Database](/plugins/alttextai/database) for filters and operators.


## Example API calls

**Read-style (read):** `account.get`

```ts
await corsair.alttextai.api.account.get({});
```


**Write-style (write):** `account.update`

```ts
await corsair.alttextai.api.account.update({});
```


See the full list on the [API](/plugins/alttextai/api) page. Use `pnpm corsair list --plugin=alttextai` and `pnpm corsair schema <path>` locally to inspect schemas.

---

## Hooks

Use `hooks` on API calls and `webhookHooks` on incoming events to add logging, approvals, or side effects. See [Hooks](/concepts/hooks) and [Webhooks](/concepts/webhooks) for routing and payload patterns.

---

## Reference

| Topic | Link |
|-------|------|
| API | [API](/plugins/alttextai/api) |
| Database | [Database](/plugins/alttextai/database) |
| Credentials | [Get credentials](/plugins/alttextai/get-credentials) |

