---
title: Database
description: "Replicate does not currently expose database entities."
---

The Replicate plugin currently exposes API operations only and does not define `replicate.db.*` entities.

Use `tenant.replicate.api.*` endpoints from the [API reference](/plugins/replicate/api) and [Overview](/plugins/replicate/overview) pages.

