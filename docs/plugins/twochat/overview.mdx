---
title: Overview
description: "TwoChat plugin for Corsair"
---

Use **TwoChat** through Corsair: one client, typed API calls, optional local DB sync.

**What you get:**

- 5 typed API operations
- 3 database entities synced for fast `.search()` / `.list()` queries

## Setup

<Steps>

<Step title="Install">
```bash
pnpm install @corsair-dev/twochat
```

</Step>

<Step title="Add the plugin">
<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { twochat } from '@corsair-dev/twochat';

export const corsair = createCorsair({
	// ... other config options,
	multiTenancy: false,
    plugins: [twochat()],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { twochat } from '@corsair-dev/twochat';

export const corsair = createCorsair({
	// ... other config options,
    multiTenancy: true,
    plugins: [twochat()],
});
```

See [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Tab>
</Tabs>

</Step>

<Step title="Get credentials">
Follow [Get Credentials](/plugins/twochat/get-credentials) if you need help getting keys.

</Step>

<Step title="Store credentials">
<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --plugin=twochat
```

Use the key names documented in [Get Credentials](/plugins/twochat/get-credentials) (for example `api_key=`, `bot_token=`, or OAuth client fields).
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --plugin=twochat --tenant=<tenantId>
```

Store per-tenant secrets after you create the tenant record. See [Multi-tenancy](/concepts/multi-tenancy).
</Tab>
</Tabs>

</Step>

</Steps>

## Authentication

Each tab shows how to register the plugin for that authentication method. The default `authType` from the plugin does not need to appear in the factory call.

<Tabs>
<Tab title="API Key (Default)">

```ts corsair.ts
twochat()
```

Store credentials with `pnpm corsair setup --plugin=twochat` (see [Get Credentials](/plugins/twochat/get-credentials) for field names). For OAuth, you typically store integration keys at the provider level and tokens per account or tenant.

More: [API Key](/concepts/api-key)

</Tab>
</Tabs>


## Query synced data

Synced entities support `corsair.twochat.db.<entity>.search()` and `.list()`. See [Database](/plugins/twochat/database) for filters and operators.


## Example API calls

**Read-style (read):** `account.getApiUsageInfo`

```ts
await corsair.twochat.api.account.getApiUsageInfo({});
```


**Write-style (write):** `contacts.createContact`

```ts
await corsair.twochat.api.contacts.createContact({});
```


See the full list on the [API](/plugins/twochat/api) page. Use `pnpm corsair list --plugin=twochat` and `pnpm corsair schema <path>` locally to inspect schemas.

---

## Hooks

Use `hooks` on API calls and `webhookHooks` on incoming events to add logging, approvals, or side effects. See [Hooks](/concepts/hooks) and [Webhooks](/concepts/webhooks) for routing and payload patterns.

---

## Reference

| Topic | Link |
|-------|------|
| API | [API](/plugins/twochat/api) |
| Database | [Database](/plugins/twochat/database) |
| Credentials | [Get credentials](/plugins/twochat/get-credentials) |

