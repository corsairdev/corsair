---
title: Overview
description: "Pagerduty plugin for Corsair"
---

Use **Pagerduty** through Corsair: one client, typed API calls, optional local DB sync, and incoming webhooks documented below.

**What you get:**

- 9 typed API operations
- 1 database entity synced for fast `.search()` / `.list()` queries
- 4 incoming webhook event types

## Setup

<Steps>

<Step title="Install">
```bash
pnpm install @corsair-dev/pagerduty
```

</Step>

<Step title="Add the plugin">
<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { pagerduty } from '@corsair-dev/pagerduty';

export const corsair = createCorsair({
	// ... other config options,
	multiTenancy: false,
    plugins: [pagerduty()],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from 'corsair';
import { pagerduty } from '@corsair-dev/pagerduty';

export const corsair = createCorsair({
	// ... other config options,
    multiTenancy: true,
    plugins: [pagerduty()],
});
```

See [Multi-tenancy](/concepts/multi-tenancy) for account isolation.

</Tab>
</Tabs>

</Step>

<Step title="Get credentials">
Follow [Get Credentials](/plugins/pagerduty/get-credentials) if you need help getting keys.

</Step>

<Step title="Store credentials">
<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --plugin=pagerduty
```

Use the key names documented in [Get Credentials](/plugins/pagerduty/get-credentials) (for example `api_key=`, `bot_token=`, or OAuth client fields).
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --plugin=pagerduty --tenant=<tenantId>
```

Store per-tenant secrets after you create the tenant record. See [Multi-tenancy](/concepts/multi-tenancy).
</Tab>
</Tabs>

</Step>

</Steps>

## Authentication

Each tab shows how to register the plugin for that authentication method. The default `authType` from the plugin does not need to appear in the factory call.

<Tabs>
<Tab title="API Key (Default)">

```ts corsair.ts
pagerduty()
```

Store credentials with `pnpm corsair setup --plugin=pagerduty` (see [Get Credentials](/plugins/pagerduty/get-credentials) for field names). For OAuth, you typically store integration keys at the provider level and tokens per account or tenant.

More: [API Key](/concepts/api-key)

</Tab>
</Tabs>

## Webhooks

This plugin registers **4** webhook handler(s). Configure your provider to POST events to your Corsair HTTP endpoint, then use `webhookHooks` in the plugin factory for custom logic.

See [Webhooks](/plugins/pagerduty/webhooks) for every event path and payload shape, and [Webhooks concept](/concepts/webhooks) for how to set up routing.


## Query synced data

Synced entities support `corsair.pagerduty.db.<entity>.search()` and `.list()`. See [Database](/plugins/pagerduty/database) for filters and operators.


## Example API calls

**Read-style (read):** `incidentNotes.list`

```ts
await corsair.pagerduty.api.incidentNotes.list({});
```


**Write-style (write):** `incidentNotes.create`

```ts
await corsair.pagerduty.api.incidentNotes.create({});
```


See the full list on the [API](/plugins/pagerduty/api) page. Use `pnpm corsair list --plugin=pagerduty` and `pnpm corsair schema <path>` locally to inspect schemas.

---

## Hooks

Use `hooks` on API calls and `webhookHooks` on incoming events to add logging, approvals, or side effects. See [Hooks](/concepts/hooks) and the [Webhooks](/plugins/pagerduty/webhooks) page for payload types.

---

## Reference

| Topic | Link |
|-------|------|
| API | [API](/plugins/pagerduty/api) |
| Database | [Database](/plugins/pagerduty/database) |
| Webhooks | [Webhooks](/plugins/pagerduty/webhooks) |
| Credentials | [Get credentials](/plugins/pagerduty/get-credentials) |

