---
title: Amplitude
description: Integrate Amplitude analytics event tracking, user management, and cohorts into your application with Corsair.
---

## Quick Start

Install the plugin:

```bash
pnpm install @corsair-dev/amplitude
```

Add the Amplitude plugin to your Corsair instance:

<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from "corsair";
import { amplitude } from "@corsair-dev/amplitude";

export const corsair = createCorsair({
    plugins: [
        amplitude(),
    ],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from "corsair";
import { amplitude } from "@corsair-dev/amplitude";

export const corsair = createCorsair({
    multiTenancy: true,
    plugins: [amplitude()],
    kek: process.env.CORSAIR_KEK!,
});
```
</Tab>
</Tabs>

Once configured, you can track events and manage analytics data:

<Tabs>
<Tab title="Solo">
```ts
// Track an event
await corsair.amplitude.api.events.upload({
    api_key: "your-api-key",
    events: [{
        event_type: "page_view",
        user_id: "user-123",
    }],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts
const tenant = corsair.withTenant("user_id");

// Track an event
await tenant.amplitude.api.events.upload({
    api_key: "your-api-key",
    events: [{
        event_type: "page_view",
        user_id: "user-123",
    }],
});
```
</Tab>
</Tabs>

## Authentication

### Supported Auth Types

The Amplitude plugin supports:
- **[`api_key`](/content/concepts/api-key)** (default) - Use an Amplitude API key

### Default Auth Type

If no `authType` is specified, the plugin defaults to **[`api_key`](/content/concepts/api-key)**.

### Configuring API Key Authentication

<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --amplitude api_key=your-api-key
```

For webhook signature verification:

```bash
pnpm corsair setup --amplitude webhook_signature=your-webhook-secret
```
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --amplitude api_key=their-api-key --tenant=user_abc123
```

For webhook signature verification:

```bash
pnpm corsair setup --amplitude webhook_signature=your-webhook-secret
```
</Tab>
</Tabs>

See [API Key Authentication](/content/concepts/api-key) for full setup details.

## Options

| Option | Type | Description |
|--------|------|-------------|
| `authType` | `'api_key'` | Authentication method (defaults to `'api_key'`) |
| `key` | `string` | API key (optional, uses database if not provided) |
| `webhookSecret` | `string` | Webhook secret for signature verification |
| `hooks` | `object` | Endpoint hooks for custom logic |
| `webhookHooks` | `object` | Webhook hooks for event handling |
| `errorHandlers` | `object` | Custom error handlers |
| `permissions` | `object` | Permission configuration for AI agent access |

### Hooks

```ts
amplitude({
    hooks: {
        events: {
            upload: {
                before: async (ctx, input) => {
                    console.log("Uploading events:", input.events.length);
                    return { ctx, input };
                },
            },
        },
    },
})
```

See [Hooks](/concepts/hooks) for complete documentation.

### Error Handling

The plugin includes built-in error handlers for common scenarios. For complete documentation, see the [Error Handlers reference](/plugins/amplitude/error-handlers).

## Usage

### Accessing the API

<Tabs>
<Tab title="Solo">
```ts
// Upload events
await corsair.amplitude.api.events.upload({
    api_key: "your-api-key",
    events: [{
        event_type: "purchase",
        user_id: "user-123",
        event_properties: { item: "Pro Plan", price: 99 },
    }],
});

// Search for a user
const user = await corsair.amplitude.api.users.search({
    user_id: "user-123",
});

// Create a cohort
await corsair.amplitude.api.cohorts.create({
    name: "Power Users",
    app_id: 12345,
    id_type: "BY_USER_ID",
    ids: ["user-1", "user-2", "user-3"],
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts
const tenant = corsair.withTenant("user_id");

// Upload events
await tenant.amplitude.api.events.upload({
    api_key: "your-api-key",
    events: [{
        event_type: "purchase",
        user_id: "user-123",
        event_properties: { item: "Pro Plan", price: 99 },
    }],
});

// Search for a user
const user = await tenant.amplitude.api.users.search({
    user_id: "user-123",
});

// Create a cohort
await tenant.amplitude.api.cohorts.create({
    name: "Power Users",
    app_id: 12345,
    id_type: "BY_USER_ID",
    ids: ["user-1", "user-2", "user-3"],
});
```
</Tab>
</Tabs>

See [API Endpoints](/plugins/amplitude/api-endpoints) for the complete reference.

### Webhooks

<Tabs>
<Tab title="Solo">
```ts app/api/webhook/route.ts
import { processWebhook } from "corsair";
import { corsair } from "@/server/corsair";

export async function POST(request: Request) {
    const headers = Object.fromEntries(request.headers);
    const body = await request.json();

    const result = await processWebhook(corsair, headers, body);
    return result.response;
}
```
</Tab>
<Tab title="Multi-Tenant">
```ts app/api/webhook/route.ts
import { processWebhook } from "corsair";
import { corsair } from "@/server/corsair";

export async function POST(request: Request) {
    const headers = Object.fromEntries(request.headers);
    const body = await request.json();

    const tenantId = new URL(request.url).searchParams.get("tenantId") ?? undefined;
    const result = await processWebhook(corsair, headers, body, { tenantId });
    return result.response;
}
```
</Tab>
</Tabs>

See [Webhooks](/plugins/amplitude/webhooks) for all available events.

### Database Access

<Tabs>
<Tab title="Solo">
```ts
const events = await corsair.amplitude.db.events.search({
    data: { event_type: "purchase" },
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts
const tenant = corsair.withTenant("org-123");

const events = await tenant.amplitude.db.events.search({
    data: { event_type: "purchase" },
});
```
</Tab>
</Tabs>

See [Database](/plugins/amplitude/database) for the complete schema.

## Examples

### Example 1: Track User Actions

```ts inngest/functions.ts
export const trackUserAction = inngest.createFunction(
    { id: "track-amplitude-event" },
    { event: "app/user-action" },
    async ({ event }) => {
        const tenant = corsair.withTenant(event.data.tenantId);

        await tenant.amplitude.api.events.upload({
            api_key: event.data.apiKey,
            events: [{
                event_type: event.data.action, // [!code highlight]
                user_id: event.data.userId, // [!code highlight]
                time: Date.now(),
                event_properties: event.data.properties,
            }],
        });
    }
);
```

### Example 2: Build a Cohort from Recent Signups

```ts inngest/functions.ts
export const buildSignupCohort = inngest.createFunction(
    { id: "build-signup-cohort" },
    { cron: "0 0 * * 1" },
    async () => {
        const recentSignups = await corsair.amplitude.db.users.search({
            data: { createdAt: { gte: new Date(Date.now() - 7 * 86400000) } }, // [!code highlight]
            limit: 1000,
        });

        await corsair.amplitude.api.cohorts.create({
            name: "Last 7 Day Signups",
            app_id: 12345,
            id_type: "BY_USER_ID",
            ids: recentSignups.data.map(u => u.user_id), // [!code highlight]
        });
    }
);
```

### Example 3: Monitor Alert Webhook

```ts corsair.ts
export const corsair = createCorsair({
    plugins: [
        amplitude({
            webhookHooks: {
                monitors: {
                    alert: {
                        after: async (ctx, result) => {
                            await inngest.send({
                                name: "amplitude/monitor-alert",
                                data: {
                                    tenantId: ctx.tenantId,
                                    monitorId: result.data.monitor_id, // [!code highlight]
                                    metricValue: result.data.metric_value,
                                },
                            });
                        },
                    },
                },
            },
        }),
    ],
});
```
