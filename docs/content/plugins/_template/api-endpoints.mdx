---
title: {{PLUGIN_DISPLAY_NAME}} API Endpoints
description: Full reference for every {{PLUGIN_DISPLAY_NAME}} endpoint available through Corsair.
---

All endpoints are available under `corsair.{{PLUGIN_CLI_NAME}}.api`. Calling any endpoint automatically syncs the response to your local database.

---

## {{RESOURCE_GROUP_1_NAME}}
<!-- Repeat this H2 block for each resource group (e.g. Repositories, Issues, Users) -->

### list

`{{PLUGIN_CLI_NAME}}.{{RESOURCE_GROUP_1_KEY}}.list`

```ts
const items = await corsair.{{PLUGIN_CLI_NAME}}.api.{{RESOURCE_GROUP_1_KEY}}.list({
    {{LIST_PARAM_EXAMPLE}}: '...',
});
```

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `{{PARAM_1_NAME}}` | `{{PARAM_1_TYPE}}` | {{PARAM_1_REQUIRED}} | {{PARAM_1_DESCRIPTION}} |
| `{{PARAM_2_NAME}}` | `{{PARAM_2_TYPE}}` | {{PARAM_2_REQUIRED}} | {{PARAM_2_DESCRIPTION}} |

---

### get

`{{PLUGIN_CLI_NAME}}.{{RESOURCE_GROUP_1_KEY}}.get`

```ts
const item = await corsair.{{PLUGIN_CLI_NAME}}.api.{{RESOURCE_GROUP_1_KEY}}.get({
    {{GET_PARAM_EXAMPLE}}: '...',
});
```

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `{{PARAM_1_NAME}}` | `{{PARAM_1_TYPE}}` | {{PARAM_1_REQUIRED}} | {{PARAM_1_DESCRIPTION}} |

---

### create

`{{PLUGIN_CLI_NAME}}.{{RESOURCE_GROUP_1_KEY}}.create`

```ts
const result = await corsair.{{PLUGIN_CLI_NAME}}.api.{{RESOURCE_GROUP_1_KEY}}.create({
    {{CREATE_PARAM_EXAMPLE}}: '...',
});
```

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `{{PARAM_1_NAME}}` | `{{PARAM_1_TYPE}}` | {{PARAM_1_REQUIRED}} | {{PARAM_1_DESCRIPTION}} |
| `{{PARAM_2_NAME}}` | `{{PARAM_2_TYPE}}` | {{PARAM_2_REQUIRED}} | {{PARAM_2_DESCRIPTION}} |

---

### update

`{{PLUGIN_CLI_NAME}}.{{RESOURCE_GROUP_1_KEY}}.update`

```ts
const result = await corsair.{{PLUGIN_CLI_NAME}}.api.{{RESOURCE_GROUP_1_KEY}}.update({
    {{UPDATE_PARAM_EXAMPLE}}: '...',
});
```

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `{{PARAM_1_NAME}}` | `{{PARAM_1_TYPE}}` | {{PARAM_1_REQUIRED}} | {{PARAM_1_DESCRIPTION}} |

---

### delete

`{{PLUGIN_CLI_NAME}}.{{RESOURCE_GROUP_1_KEY}}.delete`

```ts
await corsair.{{PLUGIN_CLI_NAME}}.api.{{RESOURCE_GROUP_1_KEY}}.delete({
    {{DELETE_PARAM_EXAMPLE}}: '...',
});
```

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `{{PARAM_1_NAME}}` | `{{PARAM_1_TYPE}}` | {{PARAM_1_REQUIRED}} | {{PARAM_1_DESCRIPTION}} |

---

## {{RESOURCE_GROUP_2_NAME}}
<!-- Add as many resource groups as the plugin has -->

### list

`{{PLUGIN_CLI_NAME}}.{{RESOURCE_GROUP_2_KEY}}.list`

```ts
const items = await corsair.{{PLUGIN_CLI_NAME}}.api.{{RESOURCE_GROUP_2_KEY}}.list({
    {{LIST_PARAM_EXAMPLE}}: '...',
});
```

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `{{PARAM_1_NAME}}` | `{{PARAM_1_TYPE}}` | {{PARAM_1_REQUIRED}} | {{PARAM_1_DESCRIPTION}} |

---
