---
title: Telegram
description: Send messages, manage chats, and handle Telegram bot events from your app or agent.
---

## Quick Start

Install the plugin:

```bash
pnpm install @corsair-dev/telegram
```

Add the Telegram plugin to your Corsair instance:

<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from "corsair";
import { telegram } from "@corsair-dev/telegram";

export const corsair = createCorsair({
    plugins: [telegram()],
    database: db,
    kek: process.env.CORSAIR_KEK!,
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from "corsair";
import { telegram } from "@corsair-dev/telegram";

export const corsair = createCorsair({
    multiTenancy: true,
    plugins: [telegram()],
    database: db,
    kek: process.env.CORSAIR_KEK!,
});
```
</Tab>
</Tabs>

## Authentication

### Supported Auth Types

- **`bot_token`** (default) - Telegram bot token

### Bot Token Setup

<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --plugin=telegram bot_token=your-bot-token
```

See [Get Credentials](/plugins/telegram/get-credentials) for step-by-step instructions.
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --plugin=telegram bot_token=their-bot-token --tenant=user_abc123
```
</Tab>
</Tabs>

See [API Key Authentication](/content/concepts/api-key) for full setup details.

## Options

| Option | Type | Description |
|--------|------|-------------|
| `authType` | `'bot_token'` | Authentication method |
| `key` | `string` | Bot token (optional, uses database if not provided) |
| `webhookSecret` | `string` | Webhook secret token (optional) |
| `hooks` | `object` | Endpoint hooks for custom logic |
| `webhookHooks` | `object` | Webhook hooks for event handling |
| `errorHandlers` | `object` | Custom error handlers |

## Usage

<Tabs>
<Tab title="Solo">
```ts
// Send a message
await corsair.telegram.api.messages.sendMessage({
    chat_id: "@mychannel",
    text: "Hello from Corsair!",
});

// Send a photo
await corsair.telegram.api.messages.sendPhoto({
    chat_id: "123456789",
    photo: "https://example.com/image.png",
    caption: "Check this out",
});

// Get chat info
const chat = await corsair.telegram.api.chat.getChat({
    chat_id: "@mychannel",
});

// Get bot info
const me = await corsair.telegram.api.updates.getMe({});

// Get updates (polling)
const updates = await corsair.telegram.api.updates.getUpdates({
    limit: 10,
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts
const tenant = corsair.withTenant("user-123");

// Send a message
await tenant.telegram.api.messages.sendMessage({
    chat_id: "@mychannel",
    text: "Hello from Corsair!",
});

// Send a photo
await tenant.telegram.api.messages.sendPhoto({
    chat_id: "123456789",
    photo: "https://example.com/image.png",
    caption: "Check this out",
});

// Get chat info
const chat = await tenant.telegram.api.chat.getChat({
    chat_id: "@mychannel",
});
```
</Tab>
</Tabs>
