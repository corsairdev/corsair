---
title: Google Sheets
description: Integrate Google Sheets spreadsheets and data management into your application with Corsair.
---

## Quick Start

Add the Google Sheets plugin to your Corsair instance:

```ts title="corsair.ts"
import { createCorsair } from "corsair";
import { googlesheets } from "corsair/plugins";

export const corsair = createCorsair({
    plugins: [
        googlesheets({
            authType: "oauth_2",
        }),
    ],
});
```

Once configured, you can access the Google Sheets API through your Corsair instance:

```ts
const rows = await corsair.googlesheets.api.sheets.getRows({
    spreadsheetId: "spreadsheet-id",
});
```

## Authentication

### Supported Auth Types

The Google Sheets plugin uses OAuth 2.0 authentication exclusively.

- **`oauth_2`** (default) - OAuth 2.0 authentication

### OAuth 2.0 Setup

Store OAuth credentials in the database using the keys API:

```ts
await corsair.googlesheets.keys.setClientId('your-client-id');
await corsair.googlesheets.keys.setClientSecret('your-client-secret');
await corsair.googlesheets.keys.setRedirectUrl('https://yourapp.com/auth/googlesheets/callback');
```

After users complete the OAuth flow, access tokens are automatically stored. Alternatively, you can provide credentials directly:

```ts title="corsair.ts"
googlesheets({
    authType: "oauth_2",
    credentials: {
        clientId: process.env.GOOGLE_SHEETS_CLIENT_ID,
        clientSecret: process.env.GOOGLE_SHEETS_CLIENT_SECRET,
    },
})
```

See [Get Credentials](/plugins/googlesheets/get-credentials) for step-by-step instructions on setting up OAuth 2.0.

See [Authentication](/concepts/auth) for details on managing credentials.

## Options

The Google Sheets plugin accepts the following configuration options:

| Option | Type | Description |
|--------|------|-------------|
| `authType` | `'oauth_2'` | Authentication method (defaults to `'oauth_2'`) |
| `credentials` | `object` | Credentials object with clientId and clientSecret |
| `hooks` | `object` | Endpoint hooks for custom logic |
| `webhookHooks` | `object` | Webhook hooks for event handling |

For complete documentation on getting credentials, see the [Plugin Credentials Guide](/guides/plugin-credentials#google-sheets).
