---
title: Typeform
description: Build and manage forms, collect responses, and react to submissions with Typeform.
---

## Quick Start

Install the plugin:

```bash
pnpm install @corsair-dev/typeform
```

Add the Typeform plugin to your Corsair instance:

<Tabs>
<Tab title="Solo">
```ts corsair.ts
import { createCorsair } from "corsair";
import { typeform } from "@corsair-dev/typeform";

export const corsair = createCorsair({
    plugins: [typeform()],
    database: db,
    kek: process.env.CORSAIR_KEK!,
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts corsair.ts
import { createCorsair } from "corsair";
import { typeform } from "@corsair-dev/typeform";

export const corsair = createCorsair({
    multiTenancy: true,
    plugins: [typeform()],
    database: db,
    kek: process.env.CORSAIR_KEK!,
});
```
</Tab>
</Tabs>

## Authentication

### Supported Auth Types

- **[`oauth_2`](/content/concepts/oauth)** (default) - OAuth 2.0 user authentication

### OAuth 2.0 Setup

<Tabs>
<Tab title="Solo">
```bash
pnpm corsair setup --typeform client_id=your-client-id client_secret=your-client-secret
pnpm corsair auth --plugin=typeform
```

See [Get Credentials](/plugins/typeform/get-credentials) for step-by-step instructions on setting up OAuth 2.0.
</Tab>
<Tab title="Multi-Tenant">
```bash
pnpm corsair setup --typeform client_id=your-client-id client_secret=your-client-secret
```
</Tab>
</Tabs>

See [OAuth 2.0 Authentication](/content/concepts/oauth) for full setup including callback handling.

## Options

| Option | Type | Description |
|--------|------|-------------|
| `authType` | `'oauth_2'` | Authentication method |
| `webhookSecret` | `string` | Webhook signing secret (optional) |
| `hooks` | `object` | Endpoint hooks for custom logic |
| `webhookHooks` | `object` | Webhook hooks for event handling |
| `errorHandlers` | `object` | Custom error handlers |

## Usage

<Tabs>
<Tab title="Solo">
```ts
// List all forms
const forms = await corsair.typeform.api.forms.list({});

// Get form responses
const responses = await corsair.typeform.api.responses.list({
    formId: "form-id",
});

// Create a form
await corsair.typeform.api.forms.create({
    title: "Customer Feedback",
    fields: [
        { type: "short_text", title: "What did you think?" },
    ],
});

// Get current user
const me = await corsair.typeform.api.me.get({});
```
</Tab>
<Tab title="Multi-Tenant">
```ts
const tenant = corsair.withTenant("user-123");

// List all forms
const forms = await tenant.typeform.api.forms.list({});

// Get form responses
const responses = await tenant.typeform.api.responses.list({
    formId: "form-id",
});

// Get current user
const me = await tenant.typeform.api.me.get({});
```
</Tab>
</Tabs>

### Query cached data

<Tabs>
<Tab title="Solo">
```ts
const forms = await corsair.typeform.db.forms.search({ data: {} });

const responses = await corsair.typeform.db.responses.search({
    data: { formId: "form-id" },
});
```
</Tab>
<Tab title="Multi-Tenant">
```ts
const tenant = corsair.withTenant("user-123");

const forms = await tenant.typeform.db.forms.search({ data: {} });

const responses = await tenant.typeform.db.responses.search({
    data: { formId: "form-id" },
});
```
</Tab>
</Tabs>
